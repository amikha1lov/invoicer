parameters:
  paths:
    - ./src
  layers:
    - name: Shared
      collectors:
        - type: directory
          value: /src/Shared/.*
    - name: Auth
      collectors:
        - type: directory
          value: /src/Auth/.*
  ruleset:
    Auth:
      - Shared